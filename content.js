'use strict';
// All lessons are independently written from the verified problem statements.
const codeBlock = (code) => `<div class="code-wrap"><div class="code-head"><span>C++17 · 核心模板</span><button class="copy-code">复制代码</button></div><pre><code>${code.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;')}</code></pre></div>`;
const formula = (text, note='') => `<div class="formula">${text}${note ? `<small>${note}</small>` : ''}</div>`;
const tip = (title,text) => `<div class="callout"><strong>${title}</strong>${text}</div>`;
const chapters = [
 {id:'base', title:'进制与位运算', short:'理解数字的另一种表达方式', icon:'01', color:'#7e97b1', tint:'#eef3f8', time:25, level:'入门', tags:['进制转换','异或','位运算'], problems:['P1143','P1469','P1100','P1017'], sections:['位权：数字为什么这样表示','正进制与负进制转换','位运算：直接操作二进制','从模板走向题目'], intro:'从十进制走到二进制，把“一个数”理解为一组位。掌握表示方法之后，很多看似需要存储和查找的问题，只需一次位运算。', body:()=>`
 <h2 id="s0">01 / 位权：数字为什么这样表示</h2><p>在基数为 b 的数制中，每一位只能取 0 到 b−1。最右一位的权值是 b⁰，向左每移动一位，权值乘 b。例如十六进制 FF 的值为 15×16+15=255。</p>${formula('(dₖ…d₁d₀)<sub>b</sub> = ∑ dᵢ · bⁱ','数码 A～F 分别表示 10～15；前导零不改变数值。')}<p>读取数码时，不必每次计算幂。依次执行 <strong>value = value × b + digit</strong>，就相当于把当前数字整体左移一位，再接上新数码。这是秦九韶算法的一个应用。</p>
 <h2 id="s1">02 / 正进制与负进制转换</h2><p>十进制转正进制，反复除以基数。余数依次是从低位到高位的数码，所以最后需要翻转。例如 13 除以 2 的余数依次为 1、0、1、1，倒过来就是 1101。</p>${codeBlock(`// 2 <= from, to <= 16；P1143 的十进制值 <= 1e9
long long parse(const string& s, int from) {
    long long x = 0;
    for (char c : s) {
        int d = c <= '9' ? c - '0' : c - 'A' + 10;
        x = x * from + d;
    }
    return x;
}
string encode(long long x, int base) {
    const string digits = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    if (x == 0) return "0";
    string s;
    while (x != 0) {
        long long r = x % base;
        x /= base;
        if (r < 0) { // 负进制：将余数调整到 [0, |base|-1]
            r -= base;
            ++x;
        }
        s += digits[r];
    }
    reverse(s.begin(), s.end());
    return s;
}`)}<p>负基数仍使用非负数码。C++ 的余数可能为负，需要调整。例如 −3 = (−2)×1−1，把余数 −1 改为 1 后，商应加 1：−3 = (−2)×2+1。恒等式保持不变，最终得到 (1101)<sub>−2</sub>。</p>${tip('边界检查','0 要单独输出“0”，否则循环不会产生任何数码。上面的 encode 对正基数要求 x≥0；对负基数允许 x 为负。P1017 还要求输出原数、等号及 (baseR)。')}
 <h2 id="s2">03 / 位运算：直接操作二进制</h2><div class="table-wrap"><table><thead><tr><th>操作</th><th>意义</th><th>典型用法</th></tr></thead><tbody><tr><td>x &amp; y</td><td>同一位都为 1 才为 1</td><td>x &amp; 1 判断奇偶</td></tr><tr><td>x | y</td><td>至少有一个 1 即为 1</td><td>合并不重叠的位段</td></tr><tr><td>x ^ y</td><td>相同为 0，不同为 1</td><td>消去成对数字</td></tr><tr><td>x &lt;&lt; k / x &gt;&gt; k</td><td>左移 / 右移</td><td>移动位段</td></tr></tbody></table></div><p>异或满足交换律、结合律，且 x⊕x=0、x⊕0=x。于是出现偶数次的数会全部抵消，只剩奇数次的那一个。P1469 不需要数组，边读边异或即可，时间 O(n)，额外空间 O(1)。</p>${codeBlock(`unsigned int answer = 0, x;
for (int i = 0; i < n; ++i) {
    cin >> x;
    answer ^= x;
}
// P1100：用明确的 32 位无符号整数
uint32_t value;
cin >> value;
uint32_t swapped = (value << 16) | (value >> 16);`)}${tip('类型决定移位行为','P1100 的输入可大于有符号 int 上限。使用 uint32_t，避免有符号数左移溢出；移位次数必须小于类型位宽。异或求唯一数也依赖“其余数均出现偶数次”的条件。')}
 <h2 id="s3">04 / 从模板走向题目</h2><p>按 P1143 → P1100 → P1469 → P1017 的顺序练习：先建立位权观念，再理解移位和掩码，最后用恒等式处理负余数。算法实验室可以查看每一次除法怎样产生一位数码。</p>`, quiz:{q:'把 −3 写成 −2 进制，应得到哪一个？',options:['11','1101','1011'],answer:1,explain:'1101 = (−2)³ + (−2)² + 1 = −8 + 4 + 1 = −3。'}},
 {id:'counting', title:'组合计数基础', short:'用有条理的计数替代逐一枚举', icon:'Cⁿ', color:'#899d7e', tint:'#f0f4eb', time:40, level:'基础',tags:['加法与乘法','组合数','容斥'],problems:['P1866','P2822','P2789','P3913','P2638','P1246'], sections:['加法、乘法与容斥','组合数与杨辉三角','隔板法与字典序排名','可达状态：直线交点数'],intro:'计数的关键是把方案分成不重不漏的部分。先判断选择是否独立、顺序是否重要，再选择乘法原理、组合数、容斥或动态规划。', body:()=>`
 <h2 id="s0">01 / 加法、乘法与容斥</h2><p><strong>分类相加，分步相乘。</strong>若两类方案互斥，总数相加；若一次方案由多个步骤组成，每个步骤的可选数确定，则各步骤的选择数相乘。重叠的分类要去重。</p>${formula('|A ∪ B| = |A| + |B| − |A ∩ B|')}<p>P3913 中，设有车的行数为 r、列数为 c。整行覆盖 rN 个格子，整列覆盖 cN 个格子，交叉位置重复统计 rc 次，因此答案为 rN+cN−rc。也可以数未被覆盖的 (N−r)(N−c) 个格子，再从 N² 中减去。</p><p>P1866 先把编号上限排序为 m₁≤…≤mₙ。前 i−1 只兔子用过的编号都不超过 mᵢ，所以第 i 只恰有 mᵢ−(i−1) 个选择。将这些数相乘，并对 10⁹+7 取模。如果某一步没有可选编号，答案为 0。</p>${formula('答案 = ∏ max(0, mᵢ − i + 1)','排序只改变处理顺序，兔子的身份仍然不同，不应再除以 n!。')}
 <h2 id="s1">02 / 组合数与杨辉三角</h2><p>排列考虑顺序，组合只考虑选中了哪些元素。n 个不同元素选 k 个的方案数为 C(n,k)=n!/[k!(n−k)!]。把某个指定元素分成“选”和“不选”两类，可以得到只含加法的递推式。</p>${formula('C(n,k) = C(n−1,k−1) + C(n−1,k)','边界 C(n,0)=C(n,n)=1；k<0 或 k>n 时为 0。')}${codeBlock(`// 模数 mod 可以是合数，不使用除法或逆元
vector<vector<int>> c(N + 1, vector<int>(N + 1));
c[0][0] = 1 % mod;
for (int i = 1; i <= N; ++i) {
    c[i][0] = 1 % mod;
    for (int j = 1; j <= i; ++j)
        c[i][j] = (c[i-1][j-1] + c[i-1][j]) % mod;
}`)}<p>P2822 只关心组合数能否被 k 整除，因此全程保存余数即可。对合法位置 j≤i 建立标记 B[i][j]=[C(i,j) mod k=0]，再建立二维前缀和。预处理 O(N²)，每个查询 O(1)。</p>${formula('S[i][j] = S[i−1][j] + S[i][j−1] − S[i−1][j−1] + B[i][j]','矩形区域中的 j>i 位置必须标为 0，不能把数组初值 0 当成合法组合数。')}${tip('取模下，除法不是普通除法','P2822 的 k 可以是合数，不能套用费马小定理求逆元。杨辉三角只需要加法，因此对任意正模数都成立。前缀和统计的是数量，不再对 k 取模。')}
 <h2 id="s2">03 / 隔板法与字典序排名</h2><p>把 a 个相同物品分给 n 个不同盒子，允许空盒且全部用完，对应 x₁+…+xₙ=a、xᵢ≥0。把 a 颗星和 n−1 个隔板排成一行，答案为 C(a+n−1,n−1)。</p><p>P2638 允许信号不用完，于是添加一个“未使用”盒子，将不等式变成等式。0 信号有 C(a+n,n) 种，1 信号有 C(b+n,n) 种，两种信号相互独立，相乘即可。</p>${formula('P2638：C(a+n,n) · C(b+n,n)','中间组合数可用 64 位保存，但两项的乘积可能超出 64 位；使用 __int128 或高精度整数。')}<p>P1246 的编码按题目示例规定：先短后长，同长再按字典序。长度为 L 的单词前面，先有 ∑ₖ₌₁ᴸ⁻¹ C(26,k) 个较短单词。对每一位枚举可放的、更小的合法字母 c；若后面还有 t 位，就增加 C(26−c,t) 个方案，其中 a 的编号为 1。最后加 1，得到从 1 开始的排名。</p>${tip('排名之前先检查合法性','字母必须严格递增，长度为 1～6，且都为小写英文字母。重复字母或逆序字母都应输出 0。')}
 <h2 id="s3">04 / 可达状态：直线交点数</h2><p>P2789 不是问最大交点数，也不是问直线排列方案数，而是问<strong>不同交点数量有多少种</strong>。无三线共点时，每对不平行的直线产生一个独立交点。把直线按平行关系分组，组大小为 s₁,s₂,…，交点数为 C(n,2)−∑C(sᵢ,2)。</p><p>设 dp[i][j] 表示 i 条直线能否产生 j 个交点。选出一组 s 条彼此平行的直线，让它们与余下 i−s 条都不平行，便新增 s(i−s) 个交点。</p>${formula('dp[i][j + s(i−s)] |= dp[i−s][j]','初始 dp[0][0]=true；枚举 1≤s≤i；答案为 dp[n] 中 true 的个数。')}<p>每一种平行分组都能按此构造；重复构造使用布尔状态合并。交点数不超过 C(n,2)，直接做的时间复杂度 O(n⁴)，空间 O(n³)，n≤25 时足够。n=4 时可达交点数为 {0,3,4,5,6}，答案为 5。</p>`,quiz:{q:'将不超过 3 个相同球放入 2 个不同盒子，允许空盒，有多少种？',options:['6','8','10'],answer:2,explain:'添加一个未使用盒子，变成 x₁+x₂+x₃=3，答案 C(5,2)=10。'}},
 {id:'gcd',title:'整除、GCD 与 LCM',short:'把公因数与周期问题化为欧几里得算法',icon:'∣',color:'#b59872',tint:'#f8f2e8',time:35,level:'基础',tags:['辗转相除','最大公约数','最小公倍数'],problems:['P1029','P1072','P1572','P4057','P2651','P2660'],sections:['整除关系与欧几里得算法','最小公倍数与分数运算','反过来求满足条件的数','用约分替代巨大的乘积'],intro:'最大公约数描述共同的因子，最小公倍数描述共同的周期。它们不仅是函数模板，也能把未知数问题转化为互质关系和质因数指数约束。',body:()=>`
 <h2 id="s0">01 / 整除关系与欧几里得算法</h2><p>a 整除 b，写作 a∣b，表示存在整数 k 使 b=ka。最大公约数 gcd(a,b) 是能同时整除 a、b 的最大正整数；gcd(a,b)=1 时称二者互质。</p><p>若 a=qb+r，同时整除 a、b 的数一定整除 r=a−qb；反过来同时整除 b、r 的数也整除 a。两组公因数完全相同，因此可以不断取余缩小问题。</p>${formula('gcd(a,b) = gcd(b,a mod b)','终止条件 gcd(a,0)=|a|。单次时间复杂度 O(log min(a,b))，额外空间 O(1)。')}${codeBlock(`long long gcdll(long long a, long long b) {
    a = abs(a); b = abs(b); // 此处不处理 LLONG_MIN
    while (b != 0) {
        long long r = a % b;
        a = b; b = r;
    }
    return a;
}
long long lcmll(long long a, long long b) {
    if (a == 0 || b == 0) return 0;
    return a / gcdll(a, b) * b; // 正整数且结果可放入 long long
}`)}<p>例如 84=30×2+24，30=24×1+6，24=6×4，最后一个非零余数是 6。C++17 已提供 std::gcd、std::lcm，位于 &lt;numeric&gt;。</p>
 <h2 id="s1">02 / 最小公倍数与分数运算</h2>${formula('gcd(a,b) · lcm(a,b) = a · b','对正整数成立。先除再乘能减少中间溢出，但最终结果仍需落在类型范围内。')}<p>P4057 的三人周期相遇时间是 lcm(lcm(a,b),c)。两数性质可迭代使用，但三数不能简单写成 abc/gcd(a,b,c)。</p><p>P1572 用整数分子、分母表示分数，避免浮点误差。对 a/b+c/d，先令 g=gcd(b,d)，得到分子 a(d/g)+c(b/g)，分母 b(d/g)，再对二者约分。每读入一个带符号的分数便累加、化简。</p>${tip('分数的统一表示','分母保持正数，符号只放在分子；分子为 0 时归一为 0/1；分母为 1 时只输出整数。不要先做整数除法，再把结果相加。P1572 的输入要识别多位数和开头的负号。')}
 <h2 id="s2">03 / 反过来求满足条件的数</h2><p>P1029 给出 gcd(P,Q)=g、lcm(P,Q)=l。若 l 不是 g 的倍数则无解；否则设 P=ga、Q=gb，得到 gcd(a,b)=1、ab=l/g。</p><p>将 l/g 分解成 k 个不同素数的幂。由于 a、b 互质，一个素数的整块幂次必须全部分给其中一边；每块有两种选择。因此有序数对个数为 2ᵏ。当 l=g 时，k=0，只有 (g,g) 一组。</p><p>P1072 则可以枚举 b₁ 的约数 x，再检验 gcd(x,a₀)=a₁ 及 lcm(x,b₀)=b₁。枚举 d≤√b₁，同时检查 d 和 b₁/d，平方根处避免重复。多组较大数据还可以按质因数指数计数：</p>${formula('min(e, α₀) = α₁，max(e, β₀) = β₁','e 为 x 中某素数的指数；α、β 分别为该素数在输入四个数中的指数。')}<p>若 α₁&lt;α₀，则 e=α₁；若 α₁=α₀，则 e≥α₁。若 β₁&gt;β₀，则 e=β₁；若 β₁=β₀，则 e≤β₁。求这些限制的交集，得到每个素数可选指数的个数，各自相乘。只需枚举 b₁ 的素因子，但首先检查 a₁∣b₁，否则直接无解。</p>
 <h2 id="s3">04 / 用约分替代巨大的乘积</h2><p>P2651 无论怎样加括号，a₂ 都在分母一侧；a₁ 总在分子侧。能把其余项全部放到分子的括号方案为 a₁ / (a₂ / a₃ / … / aₙ)，括号内部按从左到右计算。因此可行的充要条件是 a₂∣(a₁a₃…aₙ)。</p><p>必要性可以按每个素数的指数理解：把所有可调整项放入分子，得到的分子减分母指数已经最大。如果这样仍然为负，其他括号也无济于事。不计算乘积，只维护残留分母 d=a₂，依次执行 d/=gcd(d,aᵢ)（跳过 i=2），最后 d=1 即可。</p>${codeBlock(`long long d = a[1]; // 下标从 0 开始，a[1] 是第二项
for (int i = 0; i < n; ++i)
    if (i != 1) d /= std::gcd(d, a[i]);
cout << (d == 1 ? "Yes" : "No");`)}<p>P2660 用较短边作边长切正方形，每次批量切掉 x/y 个，再处理剩余 y×(x mod y) 的矩形。这与欧几里得算法相同，累计 4y⌊x/y⌋，每轮取余，时间 O(log min(x,y))。在题目的整数边长模型下，正方形边长总和为 x+y−gcd(x,y)，因而结果也可以写成 4(x+y−gcd(x,y))。</p>`,quiz:{q:'已知 gcd(P,Q)=3，lcm(P,Q)=60，有序数对有多少组？',options:['2 组','4 组','8 组'],answer:1,explain:'60/3=20=2²×5，有 2 个不同的素因子，各自整体分到 P 或 Q，得到 2²=4 组。'}},
 {id:'primes',title:'素数与筛法',short:'从单个判定走向批量预处理',icon:'p',color:'#a68d9c',tint:'#f7eff3',time:35,level:'基础',tags:['埃氏筛','线性筛','区间筛'],problems:['P3383','P1835'],sections:['素数判定：只枚举到平方根','埃氏筛与线性筛','区间筛：值大但区间短','空间与数值边界'],intro:'素数是乘法世界的基本单位。单个数可以试除，成批查询则需要筛法。根据数据的“最大值”和“区间长度”选算法，往往比背模板更重要。',body:()=>`
 <h2 id="s0">01 / 素数判定：只枚举到平方根</h2><p>素数是大于 1、只有 1 和本身两个正因子的整数。合数 n 若能分解为 ab，则 a、b 中至少一个不大于 √n。因此尝试 2 到 √n 中的整数就能判断素性，时间 O(√n)。条件写成 d≤n/d 能避免 d*d 溢出。</p>${tip('1 既不是素数，也不是合数','所有筛法都需要明确排除 0 和 1；2 是唯一的偶素数。“奇数”并不等于“素数”，例如 9 和 15。')}
 <h2 id="s1">02 / 埃氏筛与线性筛</h2><p>埃氏筛按从小到大的顺序找到未被标记的数 p，然后划去它的倍数。可从 p² 开始，因为更小的倍数已经被更小素因子处理。复杂度 O(N log log N)。算法实验室可逐步观察筛选过程。</p><p>线性筛要求<strong>每个合数只由它的最小素因子生成一次</strong>。维护素数表，枚举当前 i 与素数 p 的乘积；当 p∣i 时标记后就退出。此时更大的素数若继续乘 i，会让乘积不再由最小素因子唯一生成。</p>${codeBlock(`vector<int> primes;
vector<bool> composite(N + 1, false);
for (int i = 2; i <= N; ++i) {
    if (!composite[i]) primes.push_back(i);
    for (int p : primes) {
        if (1LL * i * p > N) break;
        composite[i * p] = true;
        if (i % p == 0) break; // 保证 p 是乘积的最小素因子
    }
}
// P3383 查询第 k 小素数：primes[k - 1]`)}<p>线性筛时间 O(N)，素数表天然有序，询问 O(1)。P3383 当前 n=10⁸、q≤10⁶：关闭 iostream 同步、使用 '\\n'，并注意数组内存。vector&lt;bool&gt; 位压缩可明显减少标记数组大小。</p>
 <h2 id="s2">03 / 区间筛：值大但区间短</h2><p>P1835 的 R 接近 2³¹，但 R−L≤10⁶。不应开大小为 R 的数组；只保存 [L,R] 的合数标记。先筛出 √R 以内的素数，再对每个 p 划去落在区间内的倍数。</p>${formula('start = max(p², ⌈L/p⌉ · p)','p² 防止把素数 p 自己划掉；下标统一使用 x−L。')}${codeBlock(`// primes 已包含所有 <= sqrt(R) 的素数
vector<bool> bad(R - L + 1, false);
for (long long p : primes) {
    long long start = max(p * p, (L + p - 1) / p * p);
    for (long long x = start; x <= R; x += p)
        bad[x - L] = true;
}
if (L == 1) bad[0] = true;
int answer = 0;
for (bool b : bad) answer += !b;`)}<p>记 D=R−L+1，常用埃氏筛预处理时，总时间约 O(√R log log √R + D log log R)，空间 O(√R+D)。核心是把“值域很大”转变为“只处理一个小窗口”。</p>
 <h2 id="s3">04 / 空间与数值边界</h2><ul><li>p*p、起点计算和倍数循环使用 long long，避免 32 位乘法溢出。</li><li>单点区间 [1,1] 返回 0，[2,2] 返回 1。</li><li>筛出的数组代表合数标记时，变量名应明确，避免 true/false 含义写反。</li><li>线性筛的 break 不是优化小技巧，而是线性复杂度证明的关键。</li></ul>`,quiz:{q:'筛区间 [2,20]，处理素数 3 时应从哪里开始标记？',options:['3','6','9'],answer:2,explain:'max(3², ceil(2/3)×3)=9。3 自己必须保留，6 已由素数 2 标记。'}},
 {id:'factor',title:'质因数分解与约数',short:'把乘法和整除转化为指数关系',icon:'∏',color:'#7f9c98',tint:'#edf5f3',time:30,level:'进阶',tags:['唯一分解','指数计数','因子和'],problems:['P1069','P1593'],sections:['算术基本定理','约数个数与约数和','细胞分裂：比较质因数指数','因子和：分解而非求幂'],intro:'一个大整数可以写成若干素数幂的乘积。此时“整除”变成指数逐项比较，“约数”变成独立选择指数，让巨大数值无需真正被计算。',body:()=>`
 <h2 id="s0">01 / 算术基本定理</h2><p>每个大于 1 的整数都能唯一表示为不同素数幂的乘积（忽略排列顺序）。例如 360=2³×3²×5。试除分解时，每找到一个因子就尽量除尽，记录除去的次数。</p>${formula('n = p₁<sup>e₁</sup> p₂<sup>e₂</sup> … pₖ<sup>eₖ</sup>')}${codeBlock(`vector<pair<long long, int>> factor(long long n) {
    vector<pair<long long, int>> f;
    for (long long p = 2; p <= n / p; ++p) {
        if (n % p != 0) continue;
        int e = 0;
        while (n % p == 0) { n /= p; ++e; }
        f.push_back({p, e});
    }
    if (n > 1) f.push_back({n, 1});
    return f;
}`)}<p>为什么最后的 n&gt;1 一定是素数？如果它仍是合数，就应有一个不大于平方根的因子，与试除结束条件矛盾。最坏时间 O(√n)，也可以只试除预先筛出的素数。</p>
 <h2 id="s1">02 / 约数个数与约数和</h2><p>n 的任一约数中，素数 pᵢ 的指数可以独立选择 0～eᵢ。由乘法原理得到约数个数；把所有约数相加，相当于展开各个等比和的乘积。</p>${formula('τ(n) = ∏(eᵢ+1)<br>σ(n) = ∏(1+pᵢ+pᵢ²+…+pᵢ<sup>eᵢ</sup>)')}<p>以 12=2²×3 为例，约数有 (2+1)(1+1)=6 个；约数和为 (1+2+4)(1+3)=28。特别地，1 的质因数分解为空，空乘积定义为 1，所以 τ(1)=σ(1)=1。</p>
 <h2 id="s2">03 / 细胞分裂：比较质因数指数</h2><p>P1069 要求 m₁<sup>m₂</sup>∣Sᵗ。只分解 m₁=∏pᵉ；目标对每个 p 的指数需求为 e·m₂。若 S 中 p 的指数为 c，那么经过 t 秒会有 c·t 个指数单位。</p>${formula('t = max<sub>p∣m₁</sub> ⌈ eₚ · m₂ / cₚ ⌉','若某个需要的 p 在 S 中没有出现（cₚ=0），该种细胞无解。')}<p>例如试管总数为 24=2³×3，细胞每秒变成 12=2²×3 个，时间至少 max(⌈3/2⌉,⌈1/1⌉)=2 秒。对每种细胞求出可行时间，再取最小值。若 m₁=1，初始的 1 个细胞已可分配，答案为 0。</p>${tip('不能先求出大数再取余','m₁^m₂ 和 S^t 都可能远超 64 位。用指数比较保留全部整除信息；对大数随意取模后再判断整除通常是错误的。整数向上取整写成 (need+c−1)/c。')}
 <h2 id="s3">04 / 因子和：分解而非求幂</h2><p>P1593 求 aᵇ 的约数和模 9901。若 a=∏pᵉ，则 aᵇ=∏pᵉᵇ，答案为 ∏(1+p+…+pᵉᵇ) mod 9901。分解 a 的开销 O(√a)，关键变成如何快速求等比和。</p><p>整数公式 (p<sup>k+1</sup>−1)/(p−1) 在模数下不能直接相除。当 p−1 与模数不互质时，分母没有逆元。下一章给出不含除法、可处理所有正模数的二分算法。b=0 时 aᵇ=1，答案为 1。</p>`,quiz:{q:'360=2³×3²×5，一共有多少个正约数？',options:['12','18','24'],answer:2,explain:'三个素数的指数分别有 4、3、2 种选择，相乘得到 24。'}},
 {id:'modular',title:'同余与快速幂',short:'在取模世界中安全地做乘法与求和',icon:'≡',color:'#9790b3',tint:'#f2f0f8',time:30,level:'进阶',tags:['模运算','快速幂','等比数列'],problems:['P1593','P1866','P2822'],sections:['同余运算与除法陷阱','二进制拆分的快速幂','同时计算幂与等比和','数值类型与模板边界'],intro:'模运算让巨大答案保持在有限范围内，快速幂让指数级数值只需对数次操作。两者结合，是计数和因子和问题的常用工具。',body:()=>`
 <h2 id="s0">01 / 同余运算与除法陷阱</h2><p>若 m∣(a−b)，就称 a、b 模 m 同余，记作 a≡b (mod m)。加、减、乘可以在每一步取模；除法需要额外条件。比如整数 6/2=3，但先模 5 再整数除法会变成 1/2=0，显然不对。</p>${formula('(a+b) mod m = ((a mod m)+(b mod m)) mod m<br>(ab) mod m = ((a mod m)(b mod m)) mod m')}<p>只有 gcd(b,m)=1 时，b 才有模 m 逆元。若 m 为素数且 b 不是 m 的倍数，可以用 b<sup>m−2</sup> 求逆元；本题单的 P2822 模数不一定为素数，P1593 的分母也可能不可逆，所以要选择不依赖除法的方法。</p>
 <h2 id="s1">02 / 二进制拆分的快速幂</h2><p>把指数写成二进制。例如 13=8+4+1，a¹³=a⁸a⁴a。不断将底数平方，每一轮看指数最低位是否为 1，是则乘入答案，然后将指数右移。循环不变量是：已累计答案 × 当前底数<sup>剩余指数</sup> 与原目标同余。</p>${codeBlock(`long long modpow(long long a, long long e, long long mod) {
    a = (a % mod + mod) % mod;
    long long ans = 1 % mod;
    while (e > 0) {
        if (e & 1) ans = (__int128)ans * a % mod;
        a = (__int128)a * a % mod;
        e >>= 1;
    }
    return ans;
}`)}<p>时间复杂度 O(log e)，额外空间 O(1)。模板要求 e≥0、mod&gt;0；指数为 0 返回 1 mod m。__int128 是 GNU C++ 扩展，洛谷常用 GCC 环境支持它。</p>
 <h2 id="s2">03 / 同时计算幂与等比和</h2><p>定义 P(n)=pⁿ，S(n)=1+p+…+pⁿ⁻¹，注意 n 表示<strong>项数</strong>。递归返回二元组 (P(n),S(n))。把 2k 项分成两半，后一半恰好是前一半乘 pᵏ。</p>${formula('P(2k)=P(k)²，S(2k)=S(k)·(1+P(k))<br>P(2k+1)=P(2k)·p，S(2k+1)=S(2k)+P(2k)','空和 S(0)=0，P(0)=1；所有操作都对 m 取模。')}${codeBlock(`// 返回 {p^n, 1+p+...+p^(n-1)} mod m
pair<long long, long long> powerSum(long long p, long long n,
                                    long long m) {
    if (n == 0) return {1 % m, 0};
    p %= m;
    auto [q, s] = powerSum(p, n / 2, m);
    long long q2 = (__int128)q * q % m;
    long long s2 = (__int128)s * (1 + q) % m;
    if (n % 2 == 0) return {q2, s2};
    return {(__int128)q2 * p % m, (s2 + q2) % m};
}
// P1593：a 的每个质因子 p，其原指数为 e
// ans = ans * powerSum(p, 1LL*e*b + 1, 9901).second % 9901;`)}<p>每层只递归一次，再用常数次乘法合并，所以时间 O(log n)、递归空间 O(log n)。与“快速幂加递归求和”相比，这个版本避免重复计算幂，且在 p≡1 (mod 9901) 时仍然正确。</p>
 <h2 id="s3">04 / 数值类型与模板边界</h2>${tip('先扩大类型，再乘法','写 1LL*a*b，或在乘法前转换为 __int128；写 long long x=a*b 并不能挽救已经在 int 中溢出的乘法。取模也无法挽救取模前的溢出。')}<p>C++ 对负数取余可能得到负值，一般用 (x%m+m)%m 归一化。这里的幂和模板针对本题单的小模数，s2+q2 与 1+q 都在 long long 范围内；若扩展到接近 64 位上限的模数，加法也需要扩大类型。</p>`,quiz:{q:'要计算 1+p+…+p¹⁰，应向 powerSum 传入的项数 n 是？',options:['10','11','12'],answer:1,explain:'从 p⁰ 到 p¹⁰ 一共 11 项。指数上界与项数差 1，是等比求和最常见的边界错误。'}},
 {id:'divisors',title:'约数统计与贡献法',short:'换一个枚举方向，让复杂度降下来',icon:'∑',color:'#ae8b77',tint:'#f7f0ea',time:30,level:'进阶',tags:['倍数枚举','贡献法','整除分块'],problems:['P2926','P1414','P1403'],sections:['由逐对比较到倍数枚举','按候选公约数计数','约数和：交换求和顺序','整除分块加速'],intro:'当直接比较每对数太慢时，把问题倒过来：固定一个因子，看它能影响哪些倍数。贡献法往往不改变答案，却彻底改变枚举的规模。',body:()=>`
 <h2 id="s0">01 / 由逐对比较到倍数枚举</h2><p>P2926 中，每头牛想知道有多少其他牛的数字能整除自己的数字。两两比较需要 O(n²)，但数值上界只有 M=10⁶。先记 cnt[d] 为数字 d 出现的次数，枚举 d 的所有倍数 v，把 cnt[d] 加入 sum[v]。</p>${codeBlock(`vector<int> sum(M + 1, 0);
for (int d = 1; d <= M; ++d) {
    if (cnt[d] == 0) continue;
    for (int v = d; v <= M; v += d)
        sum[v] += cnt[d];
}
// 对每个原数字 a[i] 输出 sum[a[i]] - 1，去掉牛自己
`)}<p>最坏遍历次数为 M/1+M/2+…+M/M=O(M log M)，空间 O(M+n)。重复值代表不同的牛，不能只记“这个数字是否出现”。最后只减去自己的一次贡献。</p>
 <h2 id="s1">02 / 按候选公约数计数</h2><p>P1414 要为每个 k 求“选 k 个数的最大可能 GCD”。枚举公约数 d，计算 c[d]=∑cnt[jd]，即有多少个数是 d 的倍数。只要 c[d]≥k，就能选出 k 个都被 d 整除的数。</p>${formula('answer[k] = max { d : c[d] ≥ k }')}<p>为什么这里不会只得到“某个公因数”而错过真正的 GCD？任意选法的 GCD=g 都有 c[g]≥k，故最优 GCD 不超过右式；右式最大的 d 对应一组可选数，其 GCD 至少为 d，因此两边相等。</p><p>实现时，不要对每个 d 再更新所有 k≤c[d]。先更新 best[c[d]]=max(best[c[d]],d)，再从 n−1 向 1 做后缀最大值 best[k]=max(best[k],best[k+1])。总复杂度 O(M log M+n)。</p>
 <h2 id="s2">03 / 约数和：交换求和顺序</h2><p>P1403 要求 1 到 n 每个数的约数个数之和。一个约数 d 会在 d、2d、3d、… 中出现，共 ⌊n/d⌋ 次。与其逐个数找约数，不如直接累计每个约数的贡献。</p>${formula('∑<sub>x=1…n</sub> τ(x) = ∑<sub>d=1…n</sub> ⌊n/d⌋')}<p>例如 n=6，贡献分别为 6、3、2、1、1、1，总计 14。直接枚举 d 是 O(n)、O(1) 空间，已经足够通过 n≤10⁶ 的原题。</p>
 <h2 id="s3">04 / 整除分块加速</h2><p>进一步观察，⌊n/d⌋ 常常在一段连续区间内不变。当前左端点 l 的商为 q=⌊n/l⌋，保持这个商的最大右端点是 r=⌊n/q⌋。这一整段的贡献为 q(r−l+1)，然后跳到 r+1。</p>${codeBlock(`long long divisorCountSum(long long n) {
    long long ans = 0;
    for (long long l = 1, r; l <= n; l = r + 1) {
        long long q = n / l;
        r = n / q;
        ans += q * (r - l + 1);
    }
    return ans;
}`)}<p>商大于 √n 时最多有 √n 个对应位置；商不大于 √n 时最多有 √n 个不同取值。因此只有 O(√n) 个块。算法实验室展示每块的左右端点和贡献，帮助检查是否存在漏项。</p>${tip('选用最合适的复杂度','P1403 的 O(n) 已足够，整除分块是可复用的进一步优化。不要因为看到了数论题就默认需要复杂模板；先对照数据范围估算操作量。')}`,quiz:{q:'n=10，当前 l=4 时，商为 2。这一块的右端点 r 是？',options:['4','5','6'],answer:1,explain:'r=floor(10/2)=5，d=4 和 d=5 的商都是 2，d=6 时商变为 1。'}},
 {id:'phi',title:'欧拉函数与区间数论',short:'用筛法统计互质与不互质的数量',icon:'φ',color:'#78919c',tint:'#eef3f5',time:35,level:'挑战',tags:['欧拉函数','区间分解','综合应用'],problems:['P3601'],sections:['欧拉函数：互质的个数','从容斥推导乘积公式','在一个大区间上求 φ','完整解题路线与自检'],intro:'将“与一个数不互质的有多少个”换成它的补集，再把欧拉函数和区间筛结合。P3601 是本题单中连接多种数论工具的一道综合题。',body:()=>`
 <h2 id="s0">01 / 欧拉函数：互质的个数</h2><p>φ(n) 表示 1≤x≤n 中与 n 互质的整数个数。约定 φ(1)=1，因为 gcd(1,1)=1。例如与 12 互质的是 1、5、7、11，所以 φ(12)=4。</p><p>P3601 定义的“不互质个数”恰好是 n−φ(n)，所以目标为对区间内每个 n 累加这个差，最后对 666623333 取模。这样就把逐对求 GCD 的问题改为批量求欧拉函数。</p>${formula('qiandao(n) = n − φ(n)')}
 <h2 id="s1">02 / 从容斥推导乘积公式</h2><p>如果 n 的不同素因子为 p₁,…,pₖ，那么与 n 不互质的数至少被其中一个素因子整除。在 1～n 中，被 p 整除的占 1/p；用容斥原理处理重叠，最终得到乘积公式。</p>${formula('φ(n) = n ∏<sub>p∣n</sub> (1 − 1/p)','每个不同素因子只处理一次，与它在 n 中的指数大小无关。')}<p>实现中每遇到一个不同素因子 p，执行 phi=phi/p*(p−1)。此时除法是整数上的精确除法，可以先除再乘。不能先对 phi 取模再执行这一步。</p>
 <h2 id="s2">03 / 在一个大区间上求 φ</h2><p>P3601 中 R≤10¹²，但 R−L≤10⁶。对每个区间位置建立两个数组：phi[x−L]=x，rem[x−L]=x。先筛出 √R 内的素数，再处理它们在区间中的所有倍数。</p>${codeBlock(`const long long MOD = 666623333;
int len = int(R - L + 1);
vector<long long> phi(len), rem(len);
for (int i = 0; i < len; ++i) phi[i] = rem[i] = L + i;
for (long long p : primes) { // 所有 <= sqrt(R) 的素数
    for (long long x = (L + p - 1) / p * p; x <= R; x += p) {
        int i = int(x - L);
        phi[i] = phi[i] / p * (p - 1);
        while (rem[i] % p == 0) rem[i] /= p;
    }
}
long long answer = 0;
for (int i = 0; i < len; ++i) {
    if (rem[i] > 1) phi[i] = phi[i] / rem[i] * (rem[i] - 1);
    answer = (answer + (L + i - phi[i]) % MOD) % MOD;
}`)}<p>处理完所有小素因子后，rem&gt;1 的部分一定是一个素数：否则它至少还有一个不超过 √R 的素因子，应该已被除尽。一定不要遗漏这一步。例如区间中的某个大素数可能根本没有被前面的循环访问。</p>${tip('这里的起点和区间素数筛不同','求 φ 时要处理素数 p 自己，所以从 ceil(L/p)×p 开始；不能使用 max(p², …)。区间素数筛是标记“合数”，这里是在分解“每一个数”。')}
 <h2 id="s3">04 / 完整解题路线与自检</h2><ol><li>用补集把不互质计数变成 n−φ(n)。</li><li>观察大值域、短区间，选择区间分解，而不是筛到 R。</li><li>每个素因子只更新一次 φ，但要把 rem 中这个因子的全部幂除尽。</li><li>检查残余大素因子，再累加并取模。</li></ol><p>记 D=R−L+1。区间访问次数约为 O(D log log R+π(√R))，重复除去素因子的开销可用 O(D log R) 作保守上界，另加小素数筛的开销；空间 O(D+√R)。R≤10¹² 时，√R≤10⁶，预处理和区间数组都可控。</p><p>边界自检：n=1 时贡献 0；n 为素数时 φ(n)=n−1，贡献 1（只有自身不互质）；n=pᵏ 时 φ(n)=n−n/p。</p>`,quiz:{q:'12 的不互质计数 qiandao(12) 是多少？',options:['4','8','11'],answer:1,explain:'φ(12)=12×(1−1/2)×(1−1/3)=4，所以不互质个数为 12−4=8。'}}
];

const problems = [
 {id:'P1143',name:'进制转换',chapter:'base',difficulty:2,tags:['位权','除基取余'],summary:'把一个 2～16 进制数转换成另一种进制。',steps:['逐位读取数码，用 value=value×原基数+数码 得到十进制值。','反复对目标基数取余，记录低位数码；商为 0 时结束，最后翻转。','按 0～9、A～F 输出数码；输入值为 0 时直接输出 0。'],example:'(FF)₁₆ → 15×16+15=255 → (11111111)₂。',complexity:'O(输入位数 + 输出位数)，额外空间 O(输出位数)。',pitfall:'不要将字符 A 的 ASCII 值当成数码 10。输入十进制值不超过 10⁹，64 位整数足够。'},
 {id:'P1469',name:'找筷子',chapter:'base',difficulty:2,tags:['异或','常数空间'],summary:'只有一个长度出现奇数次，其余筷子都能配对，求落单长度。',steps:['初始化 answer=0，边读取每根筷子的长度边执行 answer^=x。','根据异或的交换律和结合律，相同长度的偶数次出现全部抵消。','最后的 answer 就是出现奇数次的长度，不需要保存数组。'],example:'2⊕2⊕1⊕3⊕3=1。',complexity:'O(n) 时间，O(1) 额外空间。',pitfall:'原题 n 可达 10⁷+1，空间限制 8 MB；用大数组存储所有输入会超内存，读入速度也很重要。'},
 {id:'P1100',name:'高低位交换',chapter:'base',difficulty:2,tags:['移位','无符号整数'],summary:'将 32 位无符号整数的高 16 位与低 16 位交换。',steps:['使用 uint32_t 保存输入。','x<<16 将原低 16 位移动到高位，超出的原高位被丢弃。','x>>16 将原高 16 位移动到低位。两段按位或：(x<<16)|(x>>16)。'],example:'1314520 = 0x00140ED8，交换后为 0x0ED80014 = 249036820。',complexity:'O(1) 时间与空间。',pitfall:'不要使用有符号 int 保存小于 2³² 的任意非负数；位运算必须明确位宽。'},
 {id:'P1017',name:'[NOIP 2000 提高组] 进制转换',chapter:'base',difficulty:2,tags:['负进制','余数修正'],summary:'将正数或负数 n 转为基数 R∈[−20,−2] 的数码序列。',steps:['按 C++ 整数除法先求 q=n/R、r=n%R。','若 r<0，执行 r-=R、q+=1，使数码非负且 n=qR+r 仍成立。','继续转换 q，记录并翻转余数。数码 10 以上用大写字母。'],example:'−3=(−2)×2+1，2=(−2)×(−1)+0，−1=(−2)×1+1，1=(−2)×0+1，得到 1101。',complexity:'O(log|n|) 时间与输出空间；n=0 单独处理。',pitfall:'只修正负余数而不调整商会破坏恒等式。输出要严格包含 n=数码(baseR)。'},
 {id:'P1866',name:'编号',chapter:'counting',difficulty:2,tags:['排序','乘法原理'],summary:'第 i 只兔子可选 1～Mᵢ，要求所有编号不同，求方案数模 10⁹+7。',steps:['将各只兔子的编号上限升序排序。','第 i 只兔子处理前已有 i−1 个不同编号被占用，且它们一定都在 1～Mᵢ 内。','将 Mᵢ−i+1 逐项相乘取模。若该值≤0，返回 0。'],example:'上限为 5、8：先选有 5 种，再选有 7 种，答案 35。',complexity:'O(n log n) 时间；排序额外栈空间 O(log n)。',pitfall:'兔子仍是不同个体，不能除以 n!。应先检查可选数是否非正，再参与模乘。'},
 {id:'P2822',name:'[NOIP 2016 提高组] 组合数问题',chapter:'counting',difficulty:3,tags:['杨辉三角','二维前缀和'],summary:'多次查询 0≤i≤n、0≤j≤min(i,m) 中，C(i,j) 被 k 整除的个数。',steps:['预处理 0～2000 行的杨辉三角，只存模 k 的余数。','对每个合法位置建立 B[i][j]=1 当且仅当 j≤i 且余数为 0；其余位置为 0。','在完整矩形网格上建立二维前缀和。查询前缀区域 [0,n]×[0,m] 的总和即可；可以用下标偏移 1 统一处理第 0 行列。'],example:'k=2，n=m=3，只有 C(2,1)=2，答案 1。',complexity:'O(N²+t) 时间，O(N²) 空间，N=2000。',pitfall:'k 不一定是素数；j>i 不是有效组合数，不能因为数组值为 0 就计入答案。查询 m>n 时尤其容易出错。'},
 {id:'P2789',name:'直线交点数',chapter:'counting',difficulty:4,tags:['平行分组','可达性 DP'],summary:'平面上 n 条直线且无三线共点，问不同交点数共有几种可能。',steps:['按平行关系把直线分成若干组；每一对跨组直线产生一个交点。','设 dp[i][j] 为 i 条直线能否得到 j 个交点，初始 dp[0][0]=true。','枚举新平行组大小 s，向 dp[i][j+s(i−s)] 转移自 dp[i−s][j]。','最终统计 dp[n] 为 true 的位置，重复构造同一交点数只计一次。'],example:'n=4 时，交点数可为 0、3、4、5、6，共 5 种；1、2 不可达。',complexity:'直接三重枚举 O(n⁴) 时间，O(n³) 空间；n≤25。',pitfall:'问的是不同的交点“数量”，不是几何布局个数。单纯输出 C(n,2)+1 会把不可达数量计入。'},
 {id:'P3913',name:'车的攻击',chapter:'counting',difficulty:2,tags:['容斥','坐标去重'],summary:'N×N 棋盘上 K 个车覆盖所在行与列，求至少被覆盖一次的格子数。',steps:['分别统计不同的行坐标数量 r 和列坐标数量 c。可以排序后 unique 去重。','行覆盖 rN、列覆盖 cN，重复区域为 rc。','使用 64 位计算 rN+cN−rc，或 N²−(N−r)(N−c)。'],example:'N=4，车位于 (1,1)、(1,3)，r=1、c=2，覆盖 4+8−2=10 格。',complexity:'排序去重 O(K log K) 时间，O(K) 空间。',pitfall:'N 可达 10⁹，不能开棋盘或按 N 开行列数组；相同行列只计一次，乘法需先转 long long。'},
 {id:'P2638',name:'安全系统',chapter:'counting',difficulty:4,tags:['隔板法','独立计数'],summary:'n 个有标号储存区可放任意数量的 0 和 1，最多有 a 个 0、b 个 1，可不全部使用。',steps:['对 0 信号设各区数量 x₁…xₙ，有总和≤a；引入剩余数量 xₙ₊₁，使总和等于 a。','用隔板法得 C(a+n,n) 种。1 信号同理有 C(b+n,n) 种。','两种信号分配相互独立，最终相乘。组合数可用杨辉三角精确预处理。'],example:'n=2、a=b=1，答案 C(3,2)²=9。',complexity:'预处理上界 50 的组合数，O(50²) 时间和空间。',pitfall:'“最多 2 种信号”不等于“最多 2 个信号”。两项组合数的乘积可能达到约 1.6×10²⁸，需 __int128 或高精度输出。'},
 {id:'P1246',name:'[ECNA 1995] 编码',chapter:'counting',difficulty:3,tags:['组合数','字典序排名'],summary:'为长度不超过 6、字母严格递增的单词，按先长度后同长字典序的规则求排名。',steps:['检查单词由小写英文字母组成、长度≤6 且严格递增；否则输出 0。','累加所有更短长度的 C(26,len)。','逐位枚举比当前字母小、比前一位大的候选字母 c。剩余 t 位可从其后字母中选，累加 C(26−c,t)。','所有更小单词统计完后加 1，得到编码。'],example:'ab 前面有 26 个单字母，故编码 27；ac 比 ab 多一个同长前驱，编码 28。',complexity:'预处理组合数 O(26²)，排名 O(26L)，L≤6。',pitfall:'这里要遵循题目示例中的“先短后长”；不要用普通字符串字典序把 ab 排在 b 前面。'},
 {id:'P2926',name:'[USACO08DEC] Patting Heads S',chapter:'divisors',difficulty:3,tags:['频次','倍数枚举'],summary:'对每个 Aᵢ，统计其他位置 j 满足 Aⱼ∣Aᵢ 的数量。',steps:['统计每个数 d 的出现次数 cnt[d]。','枚举 d 的全部倍数 v，将 cnt[d] 累加到 sum[v]。','按输入顺序输出 sum[Aᵢ]−1，扣除自己。'],example:'输入数字 [2,1,2,3,4]，输出依次为 [2,0,2,1,3]。',complexity:'O(M log M+n) 时间，O(M+n) 空间，M=max Aᵢ≤10⁶。',pitfall:'同值的不同牛仍然相互计数；要累计频次而非仅做去重。整除方向是 Aⱼ∣Aᵢ。'},
 {id:'P3383',name:'【模板】线性筛素数',chapter:'primes',difficulty:2,tags:['线性筛','第 k 小'],summary:'给定范围 n，预处理后多次查询第 k 小的素数。',steps:['从 2 到 n 线性扫描，未标记数加入素数表。','对素数 p 依次标记 i×p 为合数；p∣i 时立即停止内层循环。','对每个查询直接输出 primes[k−1]。'],example:'n=20 的素数表是 2、3、5、7、11、13、17、19，第 5 小为 11。',complexity:'O(n+q) 时间，O(n) 标记空间（可按位压缩）及素数表。',pitfall:'当前题面已经从判素数改成第 k 小查询，且 n=10⁸。不要提交旧版输入格式；注意标记数组内存和输出缓冲。'},
 {id:'P1835',name:'素数密度',chapter:'primes',difficulty:3,tags:['区间筛','大值域'],summary:'R<2³¹、R−L≤10⁶，求 [L,R] 内素数个数。',steps:['预处理 √R 以内的全部素数。','对每个 p，从 max(p²,ceil(L/p)×p) 开始，标记区间内的倍数。','显式排除 1，统计未标记位置。'],example:'[10,20] 的素数为 11、13、17、19，共 4 个。',complexity:'区间宽度 D 下约 O(√R log log √R+D log log R) 时间，O(√R+D) 空间。',pitfall:'不能从 p 开始把素数自身标掉；用 x−L 映射下标，乘法使用 64 位。'},
 {id:'P1029',name:'[NOIP 2001 普及组] 最大公约数和最小公倍数问题',chapter:'gcd',difficulty:2,tags:['互质','质因数分配'],summary:'已知最大公约数 g 和最小公倍数 l，求满足条件的正整数有序数对数量。',steps:['若 l%g!=0，输出 0。','令 P=ga、Q=gb，则 ab=l/g 且 gcd(a,b)=1。','分解 l/g，统计不同素因子个数 k。每种素数的全部幂只能分给一边，输出 2ᵏ。'],example:'g=3、l=60，对应 (3,60)、(12,15)、(15,12)、(60,3)，共 4 组。',complexity:'试除分解 O(√(l/g)) 时间，O(1) 额外空间。',pitfall:'是有序数对，互换通常要另算；g=l 时仅一组，不要强行乘 2。'},
 {id:'P1072',name:'[NOIP 2009 提高组] Hankson 的趣味题',chapter:'gcd',difficulty:2,tags:['约数枚举','指数约束'],summary:'求 gcd(x,a₀)=a₁ 且 lcm(x,b₀)=b₁ 的正整数 x 个数。',steps:['由 x∣b₁ 可知只需考虑 b₁ 的约数，且 a₁ 必须整除 b₁。','基础方法：枚举 d≤√b₁ 的约数对，分别检查 gcd 与 lcm 条件，平方根不重复。','多组优化：筛出 √(2×10⁹) 内的素数，分解 b₁；对每个素数用 min(e,α₀)=α₁、max(e,β₀)=β₁ 求 e 的可行区间。','把各素数的可行指数个数相乘，某个区间为空则答案为 0。'],example:'a₀=6、a₁=2、b₀=4、b₁=12。约数中满足 lcm 条件的候选含 3，与 gcd(x,6)=2 冲突，答案 0。',complexity:'基础方法每组 O(√b₁+τ(b₁) log b₁)；预筛素数可加速分解与指数法。',pitfall:'输入组数可达 2000，逐整数试除四个大数会很慢。lcm 检查先除后乘并用 64 位。'},
 {id:'P1069',name:'[NOIP 2009 普及组] 细胞分裂',chapter:'factor',difficulty:4,tags:['质因数指数','向上取整'],summary:'选择一种细胞，每秒数量乘 Sᵢ，最早何时能均分到 m₁^m₂ 个试管？',steps:['只分解 m₁，将每个质因数指数 e 乘上 m₂，得到需求 need。','对每个 Sᵢ，只统计这些必需质因子的指数 c。若有 c=0，则该种细胞无解。','该细胞需要的最短时间是 max ceil(need/c)。对全部可行细胞取最小值。','m₁=1 时答案 0；所有细胞均无解时输出 −1。'],example:'M=24，S=12，2 的需求为 3、增长为 2，3 的需求为 1、增长为 1，最早 2 秒。',complexity:'分解 O(√m₁)，之后 O(n·k·log Smax) 为保守上界，k 为 m₁ 的不同素因子数。',pitfall:'不能直接计算 m₁^m₂ 或 Sᵗ；每个质因子对同一细胞都要满足，所以取最大时间而不是求和。'},
 {id:'P1572',name:'计算分数',chapter:'gcd',difficulty:2,tags:['表达式解析','约分'],summary:'精确计算只含分数加减的表达式，输出最简分数或整数。',steps:['从左至右读取符号、分子、斜杠、分母；首项默认正号，也可能以负号开头。','维护累加分数 A/B，读到带符号的 a/b 后通分相加。','每一步立刻用 gcd(|A|,B) 约分，分母保持正数。','分母为 1 输出整数，否则输出 A/B；0 规范为 0。'],example:'1/2−1/3+1/6=1/3。',complexity:'O(表达式长度+分数项数×log V) 时间，除输入串外 O(1) 空间。',pitfall:'禁止使用浮点数；要处理多位数字和负数。原题保证逐项化简后的中间结果范围，但未化简乘积应使用 long long。'},
 {id:'P4057',name:'[Code+#1] 晨跑',chapter:'gcd',difficulty:2,tags:['周期','最小公倍数'],summary:'三人分别每 a、b、c 天跑步，已在第 0 天相遇，求下一次共同跑步日。',steps:['下一次相遇日必须同时是 a、b、c 的倍数。','先求 l=lcm(a,b)，再求 lcm(l,c)。','使用 u/gcd(u,v)*v 避免不必要的中间乘积。'],example:'a=3、b=4、c=6，下一次在第 12 天。',complexity:'O(log max(a,b,c)) 时间，O(1) 空间。',pitfall:'不能用 abc/gcd(a,b,c) 代替三数最小公倍数。a,b,c≤10⁵，结果可能超过 32 位，应使用 long long。'},
 {id:'P1414',name:'又是毕业季II',chapter:'divisors',difficulty:4,tags:['倍数频次','后缀最大值'],summary:'对 k=1…n，分别求从 n 个能力值中选 k 个时的最大可能 GCD。',steps:['统计 cnt[v]，对每个候选 d 汇总 c[d]=cnt[d]+cnt[2d]+…。','若有 c[d] 个 d 的倍数，则任取不超过 c[d] 个时至少共享因子 d。','更新 best[c[d]]=max(best[c[d]],d)，忽略 c[d]=0。','对 best 做从后往前的最大值传播，输出 best[1…n]。'],example:'能力值为 1、2、3、4 时，各 k 的答案是 4、2、1、1。',complexity:'O(M log M+n) 时间，O(M+n) 空间，M≤10⁶。',pitfall:'为每个 d 更新所有 1…c[d] 会多出不必要的循环；后缀最大值一次完成传播。'},
 {id:'P2651',name:'添加括号III',chapter:'gcd',difficulty:2,tags:['整除判定','GCD 消因子'],summary:'能否给 a₁/a₂/…/aₙ 添加括号，使整个表达式是整数？',steps:['a₂ 无论怎样加括号都会出现在分母中；a₁ 总在分子中。','方案 a₁/(a₂/a₃/…/aₙ) 可以使其余项全部进入分子，所以只需判断 a₂∣a₁a₃…aₙ。','令 d=a₂，逐项用 d/=gcd(d,aᵢ) 消去分母因子，跳过 a₂。','最终 d=1 输出 Yes，否则输出 No。'],example:'1/2/1/4 可以变为 1/(2/1/4)=2，逐次消因子也将分母 2 约为 1。',complexity:'O(n log Amax) 时间；流式处理可用 O(1) 额外空间。',pitfall:'直接乘所有分子会溢出。不应枚举所有括号结构，n 可达 10⁴。'},
 {id:'P2660',name:'zzc 种田',chapter:'gcd',difficulty:2,tags:['欧几里得','批量切割'],summary:'矩形田地每次种一个正方形，体力为该正方形周长，求总最小体力。',steps:['设 x≥y，一轮取 x/y 个边长 y 的正方形，增加 4y⌊x/y⌋。','剩余矩形边长变成 y 与 x%y，继续辗转相除。','可用题目的整数边长切分结论得到等价公式 4(x+y−gcd(x,y))。'],example:'x=5、y=3：依次贡献 12、8、8，总计 28；公式为 4(5+3−1)=28。',complexity:'O(log min(x,y)) 时间，O(1) 空间。',pitfall:'x,y 可达 10¹⁶；一轮减一次的模拟会超时，需要整除批量处理，类型使用 64 位。'},
 {id:'P3601',name:'签到题',chapter:'phi',difficulty:5,tags:['欧拉函数','区间分解'],summary:'求区间内每个 x 的不互质计数之和，答案模 666623333；R≤10¹²，区间长度≤10⁶+1。',steps:['用 qiandao(x)=x−φ(x) 转化问题。','筛出 √R 以内的素数，为区间维护 phi 与 rem 两个数组。','对每个素数 p 的区间倍数更新 phi=phi/p*(p−1)，并在 rem 中除尽 p。','所有小素数处理完后，rem>1 的剩余部分作为一个大素因子，再更新一次 phi。累加各项 x−phi。'],example:'[12,12] 的答案为 12−φ(12)=12−4=8。',complexity:'小素数预筛加区间分解；反复除因子的保守上界 O(D log R)，空间 O(D+√R)。',pitfall:'不能筛到 R；不能在 phi 的精确整除步骤之前取模；更新必须从 ceil(L/p)×p 开始，以包含素数自身。'},
 {id:'P1403',name:'[AHOI2005] 约数研究',chapter:'divisors',difficulty:2,tags:['贡献法','整除分块'],summary:'求 1 到 n 中所有整数的正约数个数总和。',steps:['固定约数 d，它在 d、2d、… 中出现，共 floor(n/d) 次。','答案转化为 sum floor(n/d)，直接 O(n) 即可通过原题。','进阶优化：从 l 起令 q=n/l、r=n/q，整块累计 q(r−l+1)，再跳到 r+1。'],example:'n=3：τ(1)+τ(2)+τ(3)=1+2+2=5，也等于 3+1+1。',complexity:'基础 O(n)；整除分块 O(√n)；额外空间 O(1)。',pitfall:'分块右端点应为 n/(n/l)，每轮从 r+1 继续；累计值用 long long。'},
 {id:'P1593',name:'因子和',chapter:'modular',difficulty:4,tags:['质因数分解','分治等比和'],summary:'求 aᵇ 的所有正因子之和，对 9901 取模。',steps:['将 a 分解为 ∏pᵉ，则 aᵇ 中对应指数为 e·b。','每个质因子的贡献是 1+p+…+pᵉᵇ。用 powerSum(p,e*b+1,9901) 同时递归计算幂和等比和。','把所有素因子的贡献相乘取模；a=1 或 b=0 时答案为 1。'],example:'a=2、b=3 时 aᵇ=8，因子和 1+2+4+8=15。',complexity:'O(√a+∑log(eᵢb+1)) 时间，递归空间 O(log(eᵢb+1))。',pitfall:'不能在模运算中直接除以 p−1。分治公式不需要逆元，能覆盖 p≡1 (mod 9901) 的情况。'}
];
const difficultyNames = {2:'普及−',3:'普及 / 提高−',4:'普及+ / 提高',5:'提高+ / 省选−'};
